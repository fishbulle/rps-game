package com.example.rps.game;

public enum Result {

    // to use later instead of using the Status enum
    // so after x rounds are finished, game status will be none/finished
    // and the result will be showed in a different column in database

    WIN,
    DRAW,
    LOSE
}
